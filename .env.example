# Example environment configuration for FUTURE live market-data providers.
# Copy to .env and fill in values. NEVER commit real keys.
#
# No live provider ships with this version -- the dashboard will show
# "Live data source: Not configured" until one is implemented in
# data/live_provider.py using these variables (or Streamlit secrets).

MARKET_DATA_PROVIDER=
MARKET_DATA_API_KEY=
MARKET_DATA_API_SECRET=
MARKET_DATA_BASE_URL=
